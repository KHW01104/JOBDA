# AGENTS.md

이 문서는 이 저장소에서 작업하는 AI 코딩 에이전트의 공통 작업 규칙이다. 다른 프로젝트로 복사해 사용할 수 있도록 프로젝트에 종속되지 않는 원칙을 우선한다. 프로젝트별 규칙은 이 문서 아래에 별도로 추가한다.

## 1. 기본 역할

- 사용자의 요구사항을 먼저 이해하고, 현재 저장소의 코드와 규칙을 확인한 뒤 작업한다.
- 사용자가 명시적으로 개발을 요청하기 전에는 코드를 구현하거나 파일을 임의로 변경하지 않는다.
- 질문, 분석, 계획 요청에는 먼저 답하고 불필요한 구현을 시작하지 않는다.
- 작업은 끝까지 수행하되, 권한·인증·외부 서비스·사용자 승인 등 실제 차단 요소가 있으면 임의로 우회하지 않는다.
- 기존 사용자의 변경사항을 덮어쓰거나 되돌리지 않는다. 작업과 관련된 변경이라면 현재 상태를 이해한 뒤 함께 반영한다.
- 요청 범위를 벗어난 리팩터링, 포맷 변경, 의존성 추가를 하지 않는다.

## 2. 작업 시작 전 확인

작업을 시작하기 전에 다음을 확인한다.

1. 사용자의 요청에서 가장 구체적인 시작점인 파일, 함수, 오류, 테스트 또는 명령을 찾는다.
2. 관련된 주변 코드와 기존 테스트를 필요한 만큼만 읽는다.
3. 동작 원인에 대한 검증 가능한 가설 하나를 세운다.
4. 해당 가설을 빠르게 반증할 수 있는 가장 저렴한 검증 방법을 정한다.
5. 수정 범위와 예상되는 검증 방법을 사용자에게 짧게 공유한다.

광범위한 코드베이스 탐색을 목적 없이 계속하지 않는다. 소유하는 추상화와 가장 가까운 테스트 또는 호출부를 우선한다.

## 3. Phase 진행 규칙

- 한 번에 하나의 Phase만 진행한다.
- 현재 Phase의 구현과 검증이 완료되기 전에는 다음 Phase로 넘어가지 않는다.
- 사용자가 별도로 허용하지 않는 한 여러 Phase의 기능을 묶어서 구현하지 않는다.
- Phase의 범위가 불분명하면 구현 전에 범위와 완료 조건을 확인한다.
- Phase 완료 기준은 구현, 테스트 또는 실행 검증, 변경 요약으로 판단한다.
- 현재 Phase와 무관한 문제를 발견해도 임의로 함께 수정하지 않고 사용자에게 알린다.

## 4. 승인 기반 GitHub 작업 흐름

구현과 검증이 끝난 뒤 최종 변경사항을 사용자에게 한 번에 보고한다. 사용자가 `진행` 또는 승인을 하면 해당 작업의 GitHub 흐름을 끝까지 완료한다.

1. GitHub 이슈 생성
2. 이슈와 현재 Phase 범위 확인
3. 개발 및 로컬 검증
4. 변경사항과 검증 결과를 사용자에게 보고
5. 최종 사용자 검수 및 승인
6. 커밋 생성
7. 원격 브랜치 푸시
8. GitHub PR 작성
9. 현재 PR 템플릿 적용 내용 보고
10. PR 병합
11. 병합 결과와 이슈 종료 보고

Git의 기술적 제약상 커밋되지 않은 변경은 정상적으로 푸시할 수 없으므로 실제 실행 순서는 `커밋 -> 푸시 -> PR`이다. 최종 검수 전에는 커밋·푸시·PR·병합을 진행하지 않는다.

사용자가 수정사항을 요청하면 해당 수정사항을 반영하고 검증한 뒤 최종 검수를 다시 요청한다. 수정사항이 없고 `진행` 또는 승인이 있으면 중간 승인 없이 커밋·푸시·PR 작성·병합까지 진행한다.

### GitHub 이슈

- 이슈 제목과 본문은 작업 목적, 범위, 완료 조건이 드러나게 작성한다.
- 이슈 본문 최상단에는 `## 📌 기능 설명`을 두고, 기능 설명은 한 줄에 한 문장 또는 한 항목만 작성한 뒤 반드시 다음 줄에서 이어 쓴다. 여러 문장을 하나의 문단으로 붙여 쓰지 않아 가독성을 해치지 않는다.
- 이슈 본문은 존댓말 서술형(`~합니다`)을 사용하지 않고, `~함`, `~완료`, 명사형 등 간결한 작업 기록 문체로 작성한다.
- 구현할 세부 작업은 이슈의 To-Do 체크박스로 구체적으로 작성한다.
- 작업과 검증이 끝나 이슈를 종료하기 전, 이슈 본문을 갱신해 실제로 완료한 To-Do 항목만 `[x]`로 표시한다. 미완료 또는 제외된 항목은 체크하지 않는다.
- 이슈 종료 보고에는 체크 처리된 항목과 체크하지 않은 항목이 있다면 그 사유를 함께 명시한다.
- 이슈 제목은 PR 제목과 동일한 형식인 `[영문 유형] 한글 제목`을 사용한다.
- 이슈 제목과 PR 제목의 문구는 완전히 일치시키지 않고, 같은 작업을 설명하는 유사한 내용으로 작성한다.
- 기존 이슈 템플릿이나 저장소의 작성 규칙이 있으면 우선 사용한다.
- 이슈 번호를 알게 되면 커밋과 PR에 연결한다.

### 브랜치

- 이슈마다 전용 브랜치를 하나씩 만든다.
- 브랜치명은 `feat/#157-deployment-etcUI` 예시처럼 `유형/#이슈번호-영문설명` 형식을 사용한다.
- 브랜치 유형은 작업 성격에 맞춰 `feat`, `fix`, `refactor`, `docs`, `test`, `chore` 등을 사용한다.
- 이슈 번호와 영문 설명은 브랜치명에 반드시 포함해 이슈와 연결한다.
- `#`이 포함된 브랜치명은 셸에서 해석되지 않도록 필요한 경우 따옴표로 감싼다.

### Pull Request

- 저장소의 `.github/pull_request_template.md` 또는 기존 PR 템플릿을 반드시 사용한다.
- 사용자가 직접 수정한 현재 PR 템플릿 형식을 기준으로 작성한다.
- 사용자의 별도 승인 없이 PR 템플릿의 섹션을 추가, 삭제, 이름 변경 또는 순서 변경하지 않는다.
- PR 제목, 본문, 작성 내용은 원칙적으로 모두 한글로 작성한다.
- PR 제목은 이슈 제목과 동일한 형식인 `[영문 유형] 한글 제목`을 사용한다.
- PR 제목의 문구는 이슈 제목과 완전히 일치시키지 않고, 같은 작업을 설명하는 유사한 내용으로 작성한다.
- GitHub 자동화에 필요한 고정 키워드, 브랜치명, 파일명, 코드 식별자 등은 기능상 필요한 경우에만 원문을 유지한다.
- PR은 현재 템플릿에 정의된 항목만 작성하고, 각 항목의 내용은 실제 작업에 맞게 구체적으로 채운다.
- PR 본문에 실제로 수행하지 않은 테스트나 검증을 작성하지 않는다.
- PR 생성 후 별도 요청을 기다리지 않고, PR 링크와 함께 실제 사용한 PR 제목 및 본문 전체를 기본으로 보여준다.
- 관련 이슈 연결은 저장소 규칙에 따라 `Closes #번호` 형식을 우선 사용한다.

## 5. 커밋 메시지 규칙

커밋 메시지는 대괄호 안에 영어 유형을 쓰고, 뒤에 한글 제목을 작성한다.

```text
[Feat] 로그인 기능 구현
[Fix] 공고 중복 저장 오류 수정
[Refactor] 수집기 구조 개선
[Docs] PR 작업 규칙 문서화
[Test] 공고 정규화 테스트 추가
[Chore] 개발 환경 설정 추가
```

- 유형은 `Feat`, `Fix`, `Refactor`, `Docs`, `Test`, `Chore` 등 의미가 분명한 영어 분류를 사용한다.
- 제목은 짧고 구체적인 한글 명령형 또는 서술형으로 작성한다.
- 한 커밋에는 하나의 논리적 변경만 담는다.
- 최종 검수 승인 없이 커밋하지 않는다.
- 커밋을 만들 때 공동 작업자의 기존 변경사항을 포함하지 않도록 확인한다.
- 커밋, 브랜치 생성, 푸시, PR 작성, PR 병합은 사용자가 최종 검수 후 `진행` 또는 승인을 했을 때 수행한다.

## 6. 수정 원칙

- 표면적인 증상보다 가능한 경우 근본 원인을 수정한다.
- 가장 작은 변경으로 요구사항을 충족한다.
- 기존 공개 API, 데이터 형식, 네이밍, 폴더 구조와 일관성을 유지한다.
- 새로운 추상화는 중복이나 복잡성을 실제로 줄일 때만 추가한다.
- 구조화된 데이터는 문자열 조작보다 프로젝트와 표준 라이브러리의 파서·구조화 API를 우선한다.
- 비밀값, 비밀번호, 토큰, 개인 정보는 소스 코드나 로그에 넣지 않는다.
- 기능 구현과 무관한 주석을 추가하지 않는다. 복잡한 로직을 이해하는 데 필요한 경우에만 짧은 주석을 남긴다.
- 파일을 수정할 때는 가능한 한 작은 범위로 변경하고, 관련 없는 포맷이나 메타데이터는 건드리지 않는다.
- 새 파일 생성과 기존 파일 수정은 제공된 파일 편집 도구를 사용한다.
- 기본적으로 ASCII를 사용하고, 프로젝트가 이미 사용하는 문자 집합이나 사용자 요구로 필요한 경우에만 다른 문자를 사용한다.

## 7. 검증 규칙

- 첫 substantive edit 직후에는 반드시 해당 변경을 검증하는 가장 좁은 실행 가능한 검사를 수행한다.
- 우선순위는 다음과 같다.
  1. 실패를 빠르게 재현하거나 가설을 반증하는 동작 검증
  2. 변경된 영역의 좁은 테스트
  3. 변경된 영역의 컴파일, 타입 검사 또는 린트
  4. 실행 가능한 검증이 없을 때만 diff 확인
- 첫 검증이 실패하면 같은 범위에서 원인을 수정하고 동일 검증을 다시 실행한다.
- 검증이 현재 가설을 반증하면 가장 가까운 제어 코드로 한 단계 이동해 다시 판단한다.
- 테스트를 실행하지 못했다면 실행하지 못한 이유를 최종 보고에 명확히 적는다.
- 성공했다고 보고하는 검증은 실제로 실행한 명령과 결과만 포함한다.
- 기능 변경이 끝나면 최소 한 번의 실행 가능한 사후 검증을 남긴다.

## 8. 사용자 보고 규칙

작업 중에는 다음 내용을 간결하게 공유한다.

- 현재 확인 중인 대상과 이유
- 확인한 사실과 선택한 구현 방향
- 파일을 수정하기 직전에 수정 범위
- 수정 직후 수행할 검증
- 검증 결과와 남은 작업
- 발생한 오류와 영향 범위
- 확인하지 못했거나 추가 확인이 필요한 사항
- 작업을 막는 차단 요소와 사용자의 결정이 필요한 항목

최종 보고에는 다음을 포함한다.

- 변경된 내용
- 주요 파일 경로
- 실행한 검증과 결과
- 실행하지 못한 검증과 이유
- 남아 있는 위험 또는 사용자의 다음 승인 지점
- 발생한 오류, 미해결 항목, 추가 확인 필요 사항

실제 파일을 언급할 때는 가능한 경우 저장소 상대 경로와 클릭 가능한 링크를 사용한다.

## 9. 중단 및 승인 원칙

다음 상황에서는 임의로 진행하지 않고 사용자에게 확인한다.

- 요구사항이 서로 충돌한다.
- 현재 Phase의 범위를 넘어선 변경이 필요하다.
- 기존 사용자의 변경사항을 덮어쓸 위험이 있다.
- 외부 계정, 권한, 비밀값 또는 결제 승인이 필요하다.
- 테스트 실패가 현재 작업과 무관한 기존 문제인지 판단하기 어렵다.
- 커밋, 푸시, PR 작성 단계의 승인이 아직 없다.
- 이 문서에 정해지지 않은 규칙이나 기존 규칙으로 판단할 수 없는 상황이 발생했다.

### 미정 규칙 처리

- 사용자가 정하지 않은 규칙에 부딪히면 임의로 추측하거나 관례만으로 결정하지 않는다.
- 가능한 선택지와 각각의 영향, 추천안을 사용자에게 먼저 보고한다.
- 사용자가 결정할 때까지 해당 작업과 관련된 다음 단계로 진행하지 않는다.
- 결정된 내용은 이후 반복해서 적용할 수 있도록 이 문서 또는 적절한 프로젝트 문서에 기록한다.

## 10. 프로젝트별 추가 규칙 작성 위치

이 문서의 범용 규칙은 프로젝트 전반에 적용한다. 특정 프로젝트의 기술 스택, 실행 명령, 폴더 구조, 배포 방식, 도메인 규칙은 아래에 추가한다.

### 프로젝트별 규칙

- 프로젝트명:
- 주요 기술 스택:
- 개발 실행 명령:
- 테스트 명령:
- 린트 또는 타입 검사 명령:
- 배포 및 운영 규칙:
- 도메인상 금지사항:
